{
  "name": "polidove",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "node src/status.selfcheck.ts && node src/search.selfcheck.ts && node src/lessons.selfcheck.ts && node src/share.selfcheck.ts && node src/qr.selfcheck.ts",
    "lint": "eslint . && prettier --check .",
    "format": "prettier --write .",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "eslint": "^10.10.0",
    "prettier": "^3.9.7",
    "typescript": "~6.0.2",
    "typescript-eslint": "^8.70.0",
    "vite": "^8.3.0",
    "wrangler": "^4.134.0"
  },
  "dependencies": {
    "qrcode-generator": "^2.0.4"
  }
}
